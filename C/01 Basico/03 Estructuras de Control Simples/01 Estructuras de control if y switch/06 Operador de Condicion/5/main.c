#include <stdio.h>
#include <stdlib.h>

int main()
{
    int cantidadMaestros,cantidadAlumnos;
    cantidadMaestros=1;
    cantidadAlumnos=22;
    ((cantidadMaestros==1)&&(cantidadAlumnos>0))?printf("si hay clase =)"):
        printf("no hay clase :'(");
    printf("\n");
    return 0;
}
